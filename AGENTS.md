# Agent guidance

Use the installed, version-matched ProjectAtlas skill and MCP tools as the repository orientation layer. Run `projectatlas init` only when project-local state is absent, and follow the skill's incremental freshness policy after changes.
