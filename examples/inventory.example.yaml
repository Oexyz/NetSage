schema_version: 1

devices:
  fortigate-example:
    name: fortigate-example
    platform: fortios
    host: 192.0.2.10
    port: 22
    credential_ref: fortigate-readonly
    trust_ref: fortigate-example
    capabilities:
      - facts
      - interfaces
      - vlans
      - arp
      - routes
      - system_health
      - firewall
      - ping
      - traceroute

sites: {}
groups: {}
